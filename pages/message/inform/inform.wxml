<view class="inform_top">
  <component title='通知消息' show_bol='{{falsr}}'></component>
</view>
<view class="inform_box" wx:for="{{item}}" :key="index">
  <view class="inform_t">
    <view class="inform_tl">
      <view class="inform_timg">
        <image src="{{item.logo}}" />
      </view>
    </view>
    <view class="inform_tr">
      <view>{{item.title}}</view>
      <text>{{item.addtime}}</text>
    </view>
  </view>
  <view class="inform_f" >
    <view class="inform_fimg" wx:for="{{item.cover}}" wx:for-index="idx" >
       <image src="{{item}}" class="tupian" wx:if="{{idx<2}}"/>
       <!-- <view>{{item}}</view> -->
    </view>
  </view>
</view>
